# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Documentación local pesada (no se versiona)
docs/modulos/
docs/oficiales_abc/

# CSV crudos de cooperadoras (pesados, descargables).
# Solo se versiona el índice JSON generado por scripts/build-cooperadoras-index.sh
scripts/cooperadoras-csv/*
!scripts/cooperadoras-csv/.gitkeep
*.geojson

# Docker data volumes (bind mounts — SQLite, MinIO, etc.)
data/

# Environment files with secrets
.env
docker/grist/grist.env
docker/grist/oidc.env

# # Agent/tooling directories (not part of the project)
# _evo/
# _spec/
# .agents/
# .claude/
# .vscode/
# .windsurf/
