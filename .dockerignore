# Dependencias y artefactos locales
node_modules
dist
dist-ssr
*.local

# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

# Todo lo que empieza con . (configs, IDE, CI, skills, etc.)
.*/

# Docker propio (no se copian al build)
Dockerfile
docker-compose*.yml
# Nota: NO excluimos docker/ porque el Dockerfile copia docker/nginx.conf

# Documentación (no necesaria para el build)
docs

# Archivos sueltos no necesarios
skills-lock.json
AGENTS.md
README.md
