# Próximos pasos

1. Verificar si `scroll-fade` está disponible en el proyecto o instalarlo
2. Aplicar `scroll-fade-x md:scroll-fade-none` al `Tabs.List`
3. Agregar auto-scroll al tab activo (opcional)
4. Instalar `dropdown-menu` y refactorizar botones de crear asamblea
5. Probar en 375px y 500px en Chrome/Safari mobile
6. Verificar comportamiento en Firefox (fade estático, aceptable)
